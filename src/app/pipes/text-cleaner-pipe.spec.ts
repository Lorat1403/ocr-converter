import { TextCleanerPipe } from './text-cleaner-pipe';

describe('TextCleanerPipe', () => {
  it('create an instance', () => {
    const pipe = new TextCleanerPipe();
    expect(pipe).toBeTruthy();
  });
});
